---
title: "bc_ltc_analysis"
author: "Ngan Lyle"
date: "21/08/2020"
output: html_document
---

## Import and prepare the data
```{r results="hide"}
library(skimr)
library(dplyr)

# Import the data
# selected data
df <- read.csv("../data/BC/bc_ltc_selected_sep18.csv", na.strings="", header=TRUE)

# Remove the 3 homes that are considered "Special Units"
df <- df %>%
  filter(!grepl('Special Unit', FACILITY_NAME))

str(df)
```

```{r, echo=FALSE}
# Change datatypes as needed
listofcol = list('FACILITY_NAME', 'HCC_CODE', 'STREET_ADDRESS', 'CITY', 'POSTAL')

for (each in listofcol){
  df[, each] <- as.character(df[, each])
}

listofcol = list('RESIDENT_COUNCIL', 'FAMILY_COUNCIL', 'BEDS_PRIVATEbinary', 'rooms_shared', 'outbreak', 'hospital_act', 'community_act', 'ROOMS_SEMIbinary', 'ROOMS_MULTIbinary')
for (each in listofcol){
  df[, each] <- as.factor(df[, each])
}

# df$outbreak <- as.factor(df$outbreak)

str(df)
# dput(names(df))
```

```{r, include=FALSE}
# # Generate the data for analysis
# # Create lists of variables used to filter data for analysis

# # Selected data
num <- c("BEDS_TOTAL")
cat <- c("HLTH_AUTH", "OWNERSHIP", "ACCRED_STATUS", 
         "RESIDENT_COUNCIL", "FAMILY_COUNCIL", "BEDS_PRIVATEbinary", 
         "rooms_shared","community_act", "hospital_act", 
         "ROOMS_SEMIbinary", "ROOMS_MULTIbinary")
data <- subset(df, select = c(num, cat, 'outbreak'))

str(data)

```



```{r, }
## Assessing skewness
# Check for R skewness in numeric predictors
library(e1071)

listofcol = c(num)

for (each in listofcol){
  print(each)
  print(skewness(data[[each]]))
  qqnorm(data[[each]], main = each)
  qqline(data[[each]], col = 'red')
}

```

```{r, include=FALSE}
# Assess variables for 0s
for (each in listofcol){
  print(each)
  print(summary(df[[each]]))
}

```

All the variables above have 0s except for BEDS_TOTAL so will square root transform

## Explore the variables for transformation
```{r, fig.show="hide"}
library(rcompanion)

# Visualize the effect of square root transformations on the  predictors
for (each in listofcol) {  
  plotNormalHistogram(x = data[[each]], main = each)
  plotNormalHistogram(x = sqrt(data[[each]]), main = c(each, 'sqrt trans'))
}

```
Sqrt transformation is best for all the data.

## Transform the data
```{r }
# Square root transform in place
data[listofcol] <- sqrt(data[listofcol])

skim(data)

```

## Fit logistic regression
```{r }
# Fit logistic regression using transformed data
fit = glm(outbreak~., family=binomial, data=data)
summary(fit)
```

### Backwards Selection
```{r, include=FALSE}
# Manual
fit <- update(fit, .~. -HLTH_AUTH)
summary(fit)
fit <- update(fit, .~. -rooms_shared)
summary(fit)
fit <- update(fit, .~. -ROOMS_SEMIbinary)
summary(fit)
fit <- update(fit, .~. -ROOMS_MULTIbinary)
summary(fit)
fit <- update(fit, .~. -hospital_act)
summary(fit)
fit <- update(fit, .~. -community_act)
summary(fit)
fit <- update(fit, .~. -BEDS_PRIVATEbinary)
summary(fit)
fit <- update(fit, .~. -RESIDENT_COUNCIL)
summary(fit)
fit <- update(fit, .~. -ACCRED_STATUS)
summary(fit)
fit <- update(fit, .~. -OWNERSHIP)
summary(fit)

```

```{r }
# Backward selection using AIC
fit_back_aic = step(fit, direction = "backward")

coef(fit_back_aic)
```

```{r }
# Bacward selection using BIC
n = length((resid(fit)))
fit_back_bic = step(fit, direction = "backward", k = log(n))

coef(fit_back_bic)
```

```{r }
# A function which calculates the LOOCV RMSE
calc_loocv_rmse = function(model) {
  sqrt(mean((resid(model) / (1 - hatvalues(model))) ^ 2))
}

# Calculate LOOCV RMSE for each model
calc_loocv_rmse(fit)
calc_loocv_rmse(fit_back_aic)
calc_loocv_rmse(fit_back_bic)
```



```{r }

```


```{r }

```

### Forward Selection
```{r }
fit_start = glm(outbreak ~ 1, family=binomial, data = data)
fit_fwd_aic = step(
  fit_start, 
  scope = outbreak ~ BEDS_TOTAL + HLTH_AUTH + OWNERSHIP + 
    ACCRED_STATUS + FAMILY_COUNCIL + RESIDENT_COUNCIL + BEDS_PRIVATEbinary + rooms_shared + 
    community_act + hospital_act + ROOMS_SEMIbinary + ROOMS_MULTIbinary, 
  direction = "forward")
```

## Stepwise Selection
```{r }
fit_both_aic = step(
  fit_start, 
  scope = outbreak ~ BEDS_TOTAL + HLTH_AUTH + OWNERSHIP + 
    ACCRED_STATUS + FAMILY_COUNCIL + BEDS_PRIVATEbinary + rooms_shared + 
    community_act + hospital_act + ROOMS_SEMIbinary + ROOMS_MULTIbinary, 
  direction = "both")

fit_both_bic = step(
  fit_start, 
  scope = outbreak ~ BEDS_TOTAL + HLTH_AUTH + OWNERSHIP + 
    ACCRED_STATUS + FAMILY_COUNCIL + BEDS_PRIVATEbinary + rooms_shared + 
    community_act + hospital_act + ROOMS_SEMIbinary + ROOMS_MULTIbinary, 
  direction = "both", k = log(n))
```

### All Possible Models
```{r }
library(leaps)
all_fit = summary(regsubsets(outbreak ~ ., data = data))

# Determine the best model for a given size based on RSS
all_fit$which
all_fit$rss
all_fit$adjr2

# Model with highest adjusted R2
best_r2_ind = which.max(all_fit$adjr2)
all_fit$which[best_r2_ind,] # extract predictors

# Calculate the AIC and BIC for each of the models with the best RSS
p = length(coef(fit))
n = length(resid(fit))
fit_aic = n * log(all_fit$rss / n) + 2 * (2:p)

best_aic_ind = which.min(fit_aic)
all_fit$which[best_aic_ind,]

fit_best_aic = glm(outbreak ~ BEDS_TOTAL + HLTH_AUTH + rooms_shared, family = 'binomial', data = data)
summary(fit_best_aic)

```


```{r }
# Compare AIC for our fitted models
extractAIC(fit_best_aic)
extractAIC(fit_back_aic)
extractAIC(fit_fwd_aic)

plot(fit_aic ~ I(2:p), ylab = "AIC", xlab = "p, number of parameters", 
     pch = 20, col = "dodgerblue", type = "b", cex = 2,
     main = "AIC vs Model Complexity")
```


```{r }
# Most homes have a family_council
# 8/46 homes without a family council had an outbreak
# 26/251 with a family council had an outbreak
xtabs(~outbreak + FAMILY_COUNCIL, data=df)

# Review the minority of homes without a family council
subset(df, FAMILY_COUNCIL == 'False')
```

## Using the stepAIC() function
```{r }
library(MASS)

null <- glm(outbreak ~ 1, family = "binomial", data=data) # 1 here means the intercept 
full <- glm(outbreak ~ BEDS_TOTAL + HLTH_AUTH + OWNERSHIP + 
    ACCRED_STATUS + FAMILY_COUNCIL + RESIDENT_COUNCIL + BEDS_PRIVATEbinary + rooms_shared + 
    community_act + hospital_act + ROOMS_SEMIbinary + ROOMS_MULTIbinary, family = "binomial", data=data)

stepAIC(full, scope=list(lower=null, upper=full), data=data, direction='backward')
stepAIC(null, scope=list(lower=null, upper=full), data=data, direction='forward')
```

### LASSO
```{r }
# https://stats.stackexchange.com/questions/72251/an-example-lasso-regression-using-glmnet-for-binary-outcome
library(glmnet)

# model.matrix() automatically transforms any qualitative variables into dummy variables
# Trim the first column leaving only predictors
x = model.matrix(outbreak~., data = data)[,-1]
# y <- as.matrix(subset(data, select = c('outbreak')))
y <- data.matrix(data[,13])

# Note alpha=1 for lasso only and can blend with ridge penalty down to
# alpha=0 ridge only
glmmod <- glmnet(x, y, alpha = 1, family = 'binomial')

# Plot variable coefficients vs. shrinkage parameter lambda
plot(glmmod, xvar = 'lambda', label = TRUE)

cv.glmmod <- cv.glmnet(x, y, alpha=1, family = 'binomial')


plot(cv.glmmod)
cv.glmmod$lambda.min

coef(cv.glmmod, s = "lambda.min")

```

```{r }

```

```{r }

```
